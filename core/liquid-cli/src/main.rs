//! `liquid` agent CLI binary.
//!
//! Phase 1 scaffold. Command grammar lands in M7 (`IMPLEMENTATION_PLAN.md` §5.7).

fn main() {
    eprintln!("liquid CLI: not yet implemented (M7 — see IMPLEMENTATION_PLAN.md §5.7)");
    std::process::exit(64); // EX_USAGE
}
