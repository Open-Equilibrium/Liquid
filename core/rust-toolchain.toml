# Pinned Rust toolchain so CI, sandbox, and local environments agree.
# Bump deliberately; do not let it drift implicitly.
[toolchain]
channel = "1.94.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
